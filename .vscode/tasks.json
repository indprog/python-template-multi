{
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "command": "${workspaceFolder}/src/${fileBasename}",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0",
}