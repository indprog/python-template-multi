{
    "configurations": [
        {
            "type": "debugpy",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/src/${fileBasename}",
            "console": "integratedTerminal"
        }
    ],
    "version": "0.2.0",
}